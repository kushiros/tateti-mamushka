class fichas:
    def __init__(self, tamaño, persona):
        self.tamaño= tamaño
        self.persona=persona
        self.posicion=persona.banco

def colocar(self,grilla,ficha):
    if(ficha.tamaño < self.tamaño):
        self.posicion=ficha.posicion
        ficha.muerta()
def muerta(self):
    self.posicion=self.persona.cementerio